---
description: Produce an implementation plan from a request or spec
agent: planner
subtask: true
---

Produce an implementation plan for:

$ARGUMENTS

If a spec exists under `docs/specs/`, read it first. Write the plan to
`.opencode/plans/<slug>.md` (ask before writing). Use small, independently
verifiable slices. List open questions at the end.
