---
description: Capture a request as a reviewable spec
agent: planner
subtask: true
---

<!-- Generated by scripts/factory-sync.mjs from .factory/commands/spec.md. Edit there, not here, then run `pnpm factory:sync`. -->

Turn the request below into a spec. Write it to `docs/specs/<slug>.md` (ask
before writing in an interactive session; write directly and note it in an
unattended run), then summarize.

Request: $ARGUMENTS

Recent commits: !`git log --oneline -10`

Working tree: !`git status --short`

Include: problem, scope (in / out), acceptance criteria, affected areas
(frontend / tauri / server / crates / packages), test plan, risks, and open
questions.
