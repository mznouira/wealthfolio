---
description: Implement an approved plan slice
agent: coder
context: fork
allowed-tools: Read, Grep, Glob, Edit, Write, Bash
---

<!-- Generated by scripts/factory-sync.mjs from .factory/commands/build.md. Edit there, not here, then run `pnpm factory:sync`. -->

Implement the following slice:

$ARGUMENTS

Read the referenced plan in `.factory/plans/` first. Make surgical changes, then
run the narrowest relevant verification and report files changed, commands run,
and results.
