---
description: Produce an implementation plan from a request or spec
agent: planner
context: fork
allowed-tools: Read, Grep, Glob, Write, Edit
---

<!-- Generated by scripts/factory-sync.mjs from .factory/commands/plan.md. Edit there, not here, then run `pnpm factory:sync`. -->

Produce an implementation plan for:

$ARGUMENTS

If a spec exists under `docs/specs/`, read it first. Write the plan to
`.factory/plans/<slug>.md` (ask before writing in an interactive session; write
directly and note it in an unattended run). Use small, independently verifiable
slices. List open questions at the end.
