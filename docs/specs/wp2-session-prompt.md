---
# WP-2 session prompt (autonomous)

Paste everything below the line into a fresh orchestrator session.
---

Continue the statements work: WP-2 — PDF → positioned text. This session runs
autonomously: you are pre-authorized to make every decision until WP-2 is done.
Do not wait for approval between phases — run your full loop (spec → plan →
build → verify → review → docs) and document each decision and its rationale in
the spec and the PLAN.md session log. Stop only if blocked on something only the
owner can do (sudo for system packages, credentials/network, or a missing real
statement sample that proves essential).

Load the `statements` skill, then read docs/statements/PLAN.md (§4 architecture,
§5 WP-2 and WP-3, §8 toolchain), docs/statements/AGENTS.md,
docs/statements/CONTEXT.md, and docs/statements/NOTES.md (S2 — pdfjs-dist
feasibility; S3 — file reading; the WP-4 consumption note).

Scope:

- Add the PDF → positioned-text layer: load PDF bytes with `pdfjs-dist` and
  reconstruct `PageLine[] { page, y, cells: { x, text }[] }` from text items so
  right-aligned money lands in the correct column; handle multi-page documents.
  Institution-agnostic — no Desjardins grammar leaks in (that is WP-3; the WP-7
  upstream seam stays clean).
- Design decision to make early (in the spec): where `pdfjs-dist` lives.
  Recommendation: a subpath entry `@wealthfolio/statement-parsers/pdf` with
  pdfjs-dist as a package dependency imported only there — the main entry stays
  zero-dep and the `StatementSource` seam unchanged. Pick the current stable
  pdfjs-dist and record the version choice.
- Worker strategy per NOTES.md S2: `?worker` import →
  `GlobalWorkerOptions.workerPort` under Vite; decide the vitest/node test
  strategy (fake worker or mock — text-layer only). CSP already allows workers +
  wasm (tauri.conf.json) — zero CSP changes.
- Synthetic PDF fixture generated by a committed script (e.g. `pdf-lib` as a
  devDependency) — never a committed binary, never a real statement. If a real
  sample exists in the gitignored `statements/` folder (or another local path
  the owner uses), measure its geometry (page size, column x-positions, fonts)
  and encode that into the generator; use the real file for local manual
  verification only. If no sample is found, build the generator from the WP-3
  column layout (Date | Code | Description | Frais | Retrait | Dépôt | Solde,
  right-aligned money, wrapped descriptions, multi-page) and flag the gap in the
  session log.
- Golden test: generated synthetic PDF → expected `PageLine[]` (positions
  asserted, not just text). Plus unit tests for the reconstruction rules
  (y-clustering, x-ordering, multi-page).
- MANUAL-TESTS.md: add a dated entry for the worker under `pnpm tauri dev` (the
  real Tauri runtime — custom protocol + CSP) and `pnpm run dev:web`; first
  item: how to leave the dev server and recover if leaving fails (it occupies a
  terminal), per the fork protocol.
- If new vocabulary stabilizes (e.g. `PageLine`, cell), add it to CONTEXT.md.

Constraints: additive-only; minimal upstream edits, each declared in the spec
and session log; synthetic fixtures only — never commit a real statement,
account number, or balance; one work package this session; frontend-only (no
Rust code changes — the toolchain is available for the baseline check below).

Environment facts (do not re-derive):

- The Rust toolchain is installed and `pnpm tauri dev` works (owner-verified
  2026-09-14; versions in PLAN §8). Verify the pdfjs worker primarily under
  `pnpm tauri dev` — the real Tauri runtime — with `pnpm run dev:web` as a
  secondary check. WP-2 itself touches no Rust: run a one-time `cargo check`
  baseline (first compile is slow — fine) and record the result in the session
  log; if it fails, record the error and move on — do not debug the Rust
  toolchain in this session.
- `pnpm test`'s frontend portion has 29 pre-existing deterministic failures
  (jsdom `window.localStorage` undefined: performance-page 14,
  holdings-toolbar-order 7, spending-insights-page 8) plus timeout flake —
  verified pre-existing at `69fedca`, environmental, out of scope; do not chase.
  Your gates: the statement-parsers package tests, `pnpm lint`,
  `pnpm type-check`, `pnpm format:check`.
- The statement-parsers package (WP-1): source-distributed (exports →
  `src/index.ts`), package-level vitest (node env, explicit imports), strict
  tsconfig (`noUncheckedIndexedAccess`, `exactOptionalPropertyTypes`), root
  prettier style (printWidth 100, double quotes). 84 tests currently green —
  keep them green.
- Vite 7.3.6; `optimizeDeps.include` pattern in `apps/frontend/vite.config.ts`;
  no existing worker usage in the repo.

Finish by: updating PLAN.md's WP table (WP-2 → done) + session log (done / next
/ blocked) per the fork protocol; appending the MANUAL-TESTS.md entry; gates
green (package tests + lint + type-check + format:check; cargo baseline
recorded, not a gate); a privacy/security pass (no real statement data
anywhere); then push `zied/statements` to origin.
